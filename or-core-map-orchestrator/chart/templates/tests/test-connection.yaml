apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "map-orchestrator-service.fullname" . }}-test-connection"
  labels:
    {{- include "map-orchestrator-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "map-orchestrator-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
