import MapExporter
# Disable unit tests until we figure out how to make it work when building sysimage
# using TestEnv
# TestEnv.activate("MapExporter") do
#     include(joinpath(dirname(pathof(MapExporter)), "../test", "runtests.jl"))
# end
