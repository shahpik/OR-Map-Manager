import MapEditor
# Disable unit tests until we figure out how to make it work when building sysimage
# using TestEnv
# TestEnv.activate("MapEditor") do
#     include(joinpath(dirname(pathof(MapEditor)), "../test", "runtests.jl"))
# end
