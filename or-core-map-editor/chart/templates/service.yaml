apiVersion: v1
kind: Service
metadata:
  name: {{ include "map-editor-service.name" . }}
  labels:
    {{- include "map-editor-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "map-editor-service.selectorLabels" . | nindent 4 }}
