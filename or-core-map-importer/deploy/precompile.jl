import MapImporter
# Disable unit tests until we figure out how to make it work when building sysimage
# using TestEnv
# TestEnv.activate("MapImporter") do
#     include(joinpath(dirname(pathof(MapImporter)), "../test", "runtests.jl"))
# end
