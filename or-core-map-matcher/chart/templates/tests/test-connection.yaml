apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "map-matcher-service.fullname" . }}-test-connection"
  labels:
    {{- include "map-matcher-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "map-matcher-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
