apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-microservice-template.fullname" . }}-test-connection"
  labels:
    {{- include "python-microservice-template.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python-microservice-template.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
