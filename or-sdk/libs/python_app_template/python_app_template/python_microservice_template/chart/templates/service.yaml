apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-microservice-template.name" . }}
  labels:
    {{- include "python-microservice-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-microservice-template.selectorLabels" . | nindent 4 }}
