# Default values forpython-microservice-template.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: python-microservice-template
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: latest

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
# fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
# runAsNonRoot: true
# runAsUser: 1000

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  annotations:
    kubernetes.io/ingress.class: nginx
  #  hosts:
  #  - host: graphql.local
  #    paths: []
  #  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

app:
  port: 5082
  juliaThreads: 4

experimentManager:
  masterJWT: 06ef51e77eb04ff6a30a6d831aab1d81

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 4000m
    # memory: 4096Mi
  requests:
    cpu: 1000m
    memory: 128Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 1
  targetCPUUtilizationPercentage: 50
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

optimalReality:
  logLevel: INFO

# Settings for RDS - note password and username is set in the Makefile (pulls secrets from aws)
#postgresql:
#  database: ordb
#  endpoint: asa-atm-nonprod-optimalreality.ckoda85kvzl3.ap-southeast-2.rds.amazonaws.com:5432
#  password: postgres
#  username: postgres

# Settings for local postgres container (deployed in Kube cluster)
# postgresql:
#   database: postgres
#   endpoint: postgresql:5432
#   password: postgres
#   username: postgres

# Change endpoint to ElasticCache eventually
# redis:
#   endpoint: redis:6379

# tile38:
#   endpoint: tile38:9851