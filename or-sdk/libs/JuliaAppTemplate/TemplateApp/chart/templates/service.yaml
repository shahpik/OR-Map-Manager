apiVersion: v1
kind: Service
metadata:
  name: {{ include "julia-template-app-service.name" . }}
  labels:
    {{- include "julia-template-app-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "julia-template-app-service.selectorLabels" . | nindent 4 }}
