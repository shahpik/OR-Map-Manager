apiVersion: v1
kind: Service
metadata:
  name: {{ include "sim-metrics-service.name" . }}
  labels:
    {{- include "sim-metrics-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "sim-metrics-service.selectorLabels" . | nindent 4 }}
