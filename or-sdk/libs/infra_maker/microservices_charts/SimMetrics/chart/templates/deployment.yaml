apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sim-metrics-service.fullname" . }}
  labels:
    {{- include "sim-metrics-service.labels" . | nindent 4 }}
spec:
{{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
{{- end }}
  selector:
    matchLabels:
      {{- include "sim-metrics-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
    {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      labels:
        {{- include "sim-metrics-service.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "sim-metrics-service.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          env:
          - name: EXPERIMENT_MANAGER_ENDPOINT
            value: http://graphql-service.{{ .Release.Namespace }}
          - name: EXPERIMENT_MANAGER_WS_ENDPOINT
            value: ws://graphql-service.{{ .Release.Namespace }}
          - name: APP_PORT
            value: {{ .Values.app.port | quote }}
          - name: LOG_LEVEL
            value: {{ .Values.optimalReality.logLevel }}
          - name: OR_MASTER_JWT
            value: {{ .Values.experimentManager.masterJWT }}
          - name: JULIA_NUM_THREADS
            value: {{ .Values.app.juliaThreads | quote }}
          - name: PG_ENDPOINT
            value: {{ .Values.postgresql.endpoint }}
          - name: PG_DBNAME
            value: {{ .Values.postgresql.database }}
          - name: PG_USER
            value: {{ .Values.postgresql.username }}
          - name: PG_PASSWORD
            value: {{ .Values.postgresql.password }}
          - name: PG_SIM_ENDPOINT
            value: {{ .Values.postgresqlSimulation.endpoint }}
          - name: PG_SIM_DBNAME
            value: {{ .Values.postgresqlSimulation.database }}
          - name: PG_SIM_USER
            value: {{ .Values.postgresqlSimulation.username }}
          - name: PG_SIM_PASSWORD
            value: {{ .Values.postgresqlSimulation.password }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.app.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /live
              port: {{ .Values.app.port }}
            failureThreshold: 10
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ready
              port: {{ .Values.app.port }}
            failureThreshold: 10
            initialDelaySeconds: 5
            periodSeconds: 15
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
