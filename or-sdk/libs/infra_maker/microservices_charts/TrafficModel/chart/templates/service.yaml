apiVersion: v1
kind: Service
metadata:
  name: {{ include "traffic-model-service.name" . }}
  labels:
    {{- include "traffic-model-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "traffic-model-service.selectorLabels" . | nindent 4 }}
