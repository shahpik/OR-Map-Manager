apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "traffic-model-service.fullname" . }}-test-connection"
  labels:
    {{- include "traffic-model-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "traffic-model-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
