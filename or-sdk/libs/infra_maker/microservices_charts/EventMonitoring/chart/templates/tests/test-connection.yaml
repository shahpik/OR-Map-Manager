apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "event-monitoring-service.fullname" . }}-test-connection"
  labels:
    {{- include "event-monitoring-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "event-monitoring-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
