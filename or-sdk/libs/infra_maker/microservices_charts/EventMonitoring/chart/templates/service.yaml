apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-monitoring-service.name" . }}
  labels:
    {{- include "event-monitoring-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "event-monitoring-service.selectorLabels" . | nindent 4 }}
