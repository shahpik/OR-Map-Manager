apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-loader-service.name" . }}
  labels:
    {{- include "data-loader-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "data-loader-service.selectorLabels" . | nindent 4 }}
