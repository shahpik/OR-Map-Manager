apiVersion: v1
kind: Service
metadata:
  name: {{ include "metrics-service.name" . }}
  labels:
    {{- include "metrics-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "metrics-service.selectorLabels" . | nindent 4 }}
