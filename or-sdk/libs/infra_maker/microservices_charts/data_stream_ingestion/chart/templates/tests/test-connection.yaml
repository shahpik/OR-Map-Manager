apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-stream-ingestion-service.fullname" . }}-test-connection"
  labels:
    {{- include "data-stream-ingestion-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-stream-ingestion-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
