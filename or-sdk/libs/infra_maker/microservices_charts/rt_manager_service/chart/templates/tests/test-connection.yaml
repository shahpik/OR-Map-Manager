apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rt-manager-service.fullname" . }}-test-connection"
  labels:
    {{- include "rt-manager-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rt-manager-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
