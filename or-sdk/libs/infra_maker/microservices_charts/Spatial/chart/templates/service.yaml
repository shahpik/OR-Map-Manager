apiVersion: v1
kind: Service
metadata:
  name: {{ include "spatial-service.name" . }}
  labels:
    {{- include "spatial-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "spatial-service.selectorLabels" . | nindent 4 }}
