apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spatial-service.fullname" . }}-test-connection"
  labels:
    {{- include "spatial-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spatial-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
