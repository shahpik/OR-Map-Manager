apiVersion: v1
kind: Service
metadata:
  name: {{ include "schedule-generation-service.name" . }}
  labels:
    {{- include "schedule-generation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "schedule-generation-service.selectorLabels" . | nindent 4 }}
