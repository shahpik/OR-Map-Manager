apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "schedule-generation-service.fullname" . }}-test-connection"
  labels:
    {{- include "schedule-generation-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "schedule-generation-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
