apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-recorder-service.name" . }}
  labels:
    {{- include "data-recorder-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "data-recorder-service.selectorLabels" . | nindent 4 }}
