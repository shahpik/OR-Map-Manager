apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-recorder-service.fullname" . }}-test-connection"
  labels:
    {{- include "data-recorder-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-recorder-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
