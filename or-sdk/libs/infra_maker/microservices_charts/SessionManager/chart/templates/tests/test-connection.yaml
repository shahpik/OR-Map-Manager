apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "session-manager-service.fullname" . }}-test-connection"
  labels:
    {{- include "session-manager-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "session-manager-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
