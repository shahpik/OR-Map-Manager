apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "event-detection-service.fullname" . }}-test-connection"
  labels:
    {{- include "event-detection-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "event-detection-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
