apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-detection-service.name" . }}
  labels:
    {{- include "event-detection-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "event-detection-service.selectorLabels" . | nindent 4 }}
