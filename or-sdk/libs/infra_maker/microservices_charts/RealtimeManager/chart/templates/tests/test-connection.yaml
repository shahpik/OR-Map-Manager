apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "realtime-manager-service.fullname" . }}-test-connection"
  labels:
    {{- include "realtime-manager-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "realtime-manager-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
