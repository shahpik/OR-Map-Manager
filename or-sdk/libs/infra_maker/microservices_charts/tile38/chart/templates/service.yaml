---
apiVersion: v1
kind: Service
metadata:
  name: {{ template "name" . }}
  labels:
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
spec:
  ports:
  - port: 9851
    protocol: TCP
    name: tile38
  selector:
    app: {{ template "name" . }}